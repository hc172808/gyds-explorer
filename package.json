{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=22.18.0",
    "npm": ">=10"
  },
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "dev": "cd artifacts/solana-explorer && PORT=${PORT:-8080} BASE_PATH=${BASE_PATH:-/} vite --config vite.config.ts --host 0.0.0.0",
    "start": "cd artifacts/solana-explorer && PORT=${PORT:-8080} BASE_PATH=${BASE_PATH:-/} vite preview --config vite.config.ts --host 0.0.0.0",
    "build": "cd artifacts/solana-explorer && PORT=${PORT:-8080} BASE_PATH=${BASE_PATH:-/} ../../node_modules/.bin/vite build --config vite.config.ts",
    "build:check": "npm run typecheck && npm run build",
    "build:dev": "npm run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "bash scripts/typecheck-all.sh",
    "test": "cd artifacts/solana-explorer && ../../node_modules/.bin/vitest run --config vitest.config.ts",
    "check:decimals": "node scripts/check-decimals.mjs"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ethers": "^6.17.0"
  }
}
